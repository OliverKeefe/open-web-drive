apiVersion: v1
kind: Service
metadata:
  name: metadata-database-service
spec:
  selector:
    app: metadata-database
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
  